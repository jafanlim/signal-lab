global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: signal_lab_backend
    static_configs:
      - targets: ["host.docker.internal:4000"]
    metrics_path: /metrics
